#include  <stdio.h>

void Swap(int *pa,int *pb)
{
	int tmp=0;
	tmp=*pa;
    *pa=*pb;
	*pb=tmp;
}

int main()
{
	int a=20;
	int b=10;
	printf("a=%d,b=%d\n",a,b);
	Swap(&a,&b);
	printf("a=%d,b=%d\n",a,b);
	return 0;
}
